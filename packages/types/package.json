{
  "name": "types",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "source": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "pnpm barrel && tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "dev": "tsc -w",
    "lint": "eslint \"src/**/*.ts*\"",
    "test": "jest",
    "barrel": "barrelsby -D -d ./src"
  },
  "jest": {
    "preset": "jest-presets/jest/node"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^18.11.9",
    "barrelsby": "^2.8.1",
    "eslint": "^7.32.0",
    "eslint-config": "workspace:*",
    "jest": "^26.6.3",
    "jest-presets": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^4.8.3"
  }
}
